export const metadata = {
  title: "All Projects & Case Studies",
  description: "Explore all full-stack, frontend, backend, and clone projects built by Shibin Siyad.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
